# Directive queue — SUPERSEDED 2026-05-19

This directive queue is retired as part of the **master-blaster vault unification**. Per-repo Claudes have been replaced by a single unified orchestrator-Claude at user-home; the cross-repo coordination this queue mediated now happens in-orchestrator.

## Re: the pending Greek Constraint Catalog v1 directive (2026-05-17-1900)

The pending directive proposed reindexing R-rules into the Stage-1/2/3 architecture's Stage 2 (constraint catalog audit). **That architecture itself was retired the next day** (atu-method docs rewrite, 2026-05-18, commit `1d10aa2`) in favor of the **mechanical-first pipeline** (v0 → v1 BHSA/PROIEL clause extraction → v1.5 binding rules → v2 optional LLM → v3 editorial review). The Stage-1-LLM-primary architecture's 8-chapter validation was deemed a fluke; mechanical-first now produces ATU drafts at 85-91% boundary F1 across genres.

**Implication for the Greek pipeline**: the WORK the directive proposed (catalog the R-rules per the new architecture) is still relevant in a different form — a `binding-rules-greek.md` catalog for the mechanical-first pipeline, parallel to atu-method's `binding-rules-hebrew.md`. This is part of the multi-corpus arc (GNT-via-PROIEL pilot), tracked at `~/.claude/projects/C--Users-bibleman/memory/_named_arcs.md` and planned for Phase 7+ of master-blaster.

**Do NOT execute the original directive verbatim** — its proposed architecture (Stage-2 catalog runner, verdict-family/tier system, etc.) was abandoned. Reference the proposal as historical context; design fresh per current methodology.
